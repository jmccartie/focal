%p#notice= notice

%p
  %b Title:
  = @page.title
%p
  %b Body:
  = @page.body

- if current_user
  = link_to 'Edit', edit_page_path(@page)
  \|
  = link_to "Delete", page_path(@page), method: :delete, confirm: "Are you sure?"